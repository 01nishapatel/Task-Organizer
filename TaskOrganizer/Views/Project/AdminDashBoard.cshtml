@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@model Tuple<IEnumerable<Database.Project>,IEnumerable<Database.Task>,IEnumerable<Database.User>>
@{
    ViewData["Title"] = "AdminDashBoard";
    Layout = "~/Pages/_Layout.cshtml";
    bool isadmin = true;
}
@{
    var userid = @HttpContextAccessor.HttpContext.Session.GetInt32("UserId");
    foreach (var user in Model.Item3)
    {
        if (user.UserID == userid)
            isadmin = user.IsAdmin;
    }
}
@{
    int c1 = 0;
    foreach (var proj in Model.Item1)
    {
        if (proj.UserID == userid)
        {
            c1++;
        }
    }
}
@{
    int c2 = 0;
    foreach (var task in Model.Item2)
    {
        c2++;
    }
}@{
    int c3 = 0;
    foreach (var user in Model.Item3)
    {
        c3++;
    }
}
<div class="content-body">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3">
                <div class="card text-white bg-danger align-items-center justify-content-between">
                    <div class="card-header">
                        <h3 class="text-white">All Projects</h3>
                    </div>
                    <div class="card-body">
                        <h2 class="text-white">
                            @c1
                        </h2>
                    </div>
                </div>
            </div>



            <div class="col-sm-3">
                <div class="card text-white bg-info align-items-center justify-content-between">
                    <div class="card-header">
                        <h3 class="text-white">All Tasks</h3>
                    </div>
                    <div class="card-body">
                        <h2 class="text-white">
                            @c2
                        </h2>
                    </div>
                </div>
            </div>




            <div class="col-sm-3">
                <div class="card text-white bg-success align-items-center justify-content-between">
                    <div class="card-header">
                        <h3 class="text-white">All Users</h3>
                    </div>
                    <div class="card-body">
                        <h2 class="text-white">
                            @c3
                        </h2>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>